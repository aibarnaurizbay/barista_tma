name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Скрипт будет запускаться при каждом пуше в main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          
      - name: Install dependencies
        run: flutter pub get

      - name: Build Web
        # ЗАМЕНИ barista_tma на название своего репозитория, если оно другое
        run: flutter build web --release --base-href /barista_tma/

      - name: Upload Artifacts
        uses: actions/upload-pages-artifact@v2
        with:
          path: build/web

  deploy:
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2